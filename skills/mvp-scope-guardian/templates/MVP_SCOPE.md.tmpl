# MVP Scope -- {{project_name}}

> 🔒 **Locked on {{locked_at_human}}** by MVP Scope Guardian
> Project slug: `{{project_slug}}` · Phase: `{{current_phase}}` · Launch target: {{launch_target_display}}
>
> This document is a contract with future-you. Changes require running `scope_tool.py rescope` with a recorded rationale -- not a vibes-based edit.

---

## The Commitment

**I am building these {{launch_blocking_count}} features for launch. Nothing else.**

{{launch_blocking_table}}

### Effort summary

- **S** (<1 day) × {{effort_s_count}}
- **M** (1-3 days) × {{effort_m_count}}
- **L** (1-2 weeks) × {{effort_l_count}}
- **XL** (2+ weeks) × {{effort_xl_count}}

{{effort_warning_block}}

---

## What I Am Explicitly NOT Building

These are not oversights. These are **recorded NOs**. If a future version of me tries to add one of these mid-build, this is where past-me warned them why.

{{wont_build_block}}

---

## Escalation Path

If you genuinely need to add a feature to this list, you have exactly two legitimate moves:

1. **Trade**: Run `scope_tool.py rescope` and swap the new feature for something currently in LAUNCH-BLOCKING. You leave with the same total feature count.
2. **File it**: Add it to the parking lot or post-launch v1.1 queue in `MVP_PARKING_LOT.md`.

**Not legitimate** (the creep patterns to watch for):
- "It'll only take an hour"
- "A competitor just shipped it"
- "It would be easy to add while I'm in there"
- "I thought of a cool feature"

If you find yourself reaching for one of these, stop. Read the `MVP_PARKING_LOT.md` instead.

---

## Sign-off

By committing this file to the repo, I'm agreeing that shipping the features above = launch. Not "launch plus a few small things." Not "launch when it's perfect." **Launch.**

Signed (in spirit): past-me, {{locked_at_human}}

---

_Generated by `mvp-scope-guardian` · schema v{{schema_version}} · Re-render with `scope_tool.py render {{project_slug}}`._
