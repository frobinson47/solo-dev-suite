# Pricing -- {{project_name}}

> 💰 **Active: Version {{active_version}}** · Last updated {{updated_at_human}}
> Project slug: `{{project_slug}}` · Strategy: **{{strategy_display}}**
>
> This document is generated from `<suite>/profiles/{{project_slug}}.pricing.json`. Edit via `pricing_tool.py iterate`, never by hand.

---

## The Offer

{{offer_block}}

---

## Paid Tiers

Listed anchor-high (highest tier first) -- this is the recommended order for your pricing page as well.

{{tiers_block}}

---

## Billing Model

- **Strategy**: {{strategy_display}}{{trial_block}}
- **Billing unit**: {{billing_unit}}
- **Value metric**: {{value_metric}}
- **Annual discount**: {{annual_discount_percent}}% off (≈ {{annual_months_free}} months free vs monthly)

{{launch_strategy_block}}

---

## Competitive Anchors

What the market looks like at pricing-lock time. These are decision context, not targets to match.

{{competitors_block}}

---

## Stripe Wiring Hints

Suggested Stripe product/price structure to map from this document:

{{stripe_hints_block}}

Implementation note: use Stripe **Products** (one per tier) with **two Prices each** (monthly + annual). Store the tier's `name` in product metadata as `tier_name` so webhook handlers can route on a stable key rather than an opaque price ID.

---

## Version History

{{version_history_block}}

---

## Change Log

{{change_log_block}}

---

_Generated by `saas-pricing-architect` · schema v{{schema_version}} · Re-render with `pricing_tool.py render {{project_slug}}`._
